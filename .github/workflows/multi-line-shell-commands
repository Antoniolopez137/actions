name: multi-line shell commands
on: workflow_dispatch
jobs: 
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print multiple echo's
        run: |
            echo "First output"
            echo "Second output"
